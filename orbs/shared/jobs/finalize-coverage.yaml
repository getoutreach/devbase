description: Uploads coverage to Coveralls
executor:
  name: testbed-docker
docker:
  - image: gcr.io/outreach-docker/bootstrap/ci:stable
    auth:
      username: _json_key
      password: $GCLOUD_SERVICE_ACCOUNT
steps:
  - setup_environment
  - run:
      name: Deprecated
      command: echo "this step has been deprecated and is now a no-op"
