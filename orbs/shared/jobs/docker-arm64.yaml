description: Build and (optionally push) an ARM64 Docker image. Pushes only on release (or default) branches
executor:
  name: testbed-machine
resource_class: arm.xlarge
steps:
  - setup_environment:
      machine: true
  - build_docker_image
