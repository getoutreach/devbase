description: Build an ARM64 Docker image.
executor:
  name: testbed-machine
resource_class: arm.xlarge
steps:
  - setup_environment:
      machine: true
  - skip_on_chore_pr
  - build_docker_image:
      arch: arm64
